services:
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
    container_name: mat-frontend
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_started
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    container_name: mat-backend
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=Host=79.174.88.21;Port=16396;Database=mat;Username=mat;Password=admin_Mat123
      - JWT_SECRET=${JWT_SECRET:-MastersStockSuperSecretKey2026ForJWTTokens!}
      - Jwt__Secret=${JWT_SECRET:-MastersStockSuperSecretKey2026ForJWTTokens!}
      - Jwt__Issuer=WorkshopApi
      - Jwt__Audience=WorkshopApp
      - ALLOWED_ORIGINS=https://masters-stock.ru,http://masters-stock.ru,http://130.49.149.174
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
